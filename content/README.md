# Content models

Typed models for the Bukan Pipe platform live in `content/models/`.

Until a CMS is approved, verified records should be added as structured data under `data/`. Do not invent factory facts, products, projects, certificates, or testimonials to fill these models.

Future CMS mapping should preserve these field names so migration stays mechanical.
