# Public site origin used for canonical URLs, sitemap, and Open Graph.
# Do not treat this as confirmation of production domain ownership.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Indexing is disabled until launch is approved.
# Set to "true" only when the Project Lead explicitly allows crawlers.
NEXT_PUBLIC_ALLOW_INDEXING=false
